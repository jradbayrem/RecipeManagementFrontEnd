import { SummarizePipePipe } from './summarize-pipe.pipe';

describe('SummarizePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SummarizePipePipe();
    expect(pipe).toBeTruthy();
  });
});
